# Database connection string
# Local PostgreSQL connection (example using Docker Compose service)
# Replace values (username/password/host/port/dbname) as appropriate for your environment.
# SECURITY WARNING: Never commit actual credentials to version control!
# This .env file should be in .gitignore and only contains example format.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/the_stewards_list"

# If you use a different user/password/host/port, update the DATABASE_URL accordingly.
